// 아이템이 떨어지고 있을 때
 (항목->액티브인 경우) {
 항목->y += 5;
 // 패들과 아이템이 충돌하면 패들 크기 증가
 if (항목->y + 항목->높이 >= paddle->y & &
 품명->x + 품명->폭->= 패들->x & &
 항목->x <= 패들->x + 패들->폭) {
 항목->액티브 = false;
 패들->폭 += 20;
 }
 // 아이템이 화면 바닥에 닿으면 사라짐

 닫기(window, 렌더러);

 반환 0;
}

